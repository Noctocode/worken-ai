/** Shared route constants so deep links don't drift across components
 *  + redirects. */

/** The My Account tab on the management page — where a user switches
 *  their profile type (personal ↔ company). */
export const MY_ACCOUNT_ROUTE = "/teams?tab=my-account";
